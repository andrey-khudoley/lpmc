---
# Разворачивает узел LPMC. Каркас: наполняется вместе с ролью lpmc_system.
# Общие серверные роли (nginx, nftables, base_system…) при необходимости
# подключаются из инфра-репозитория, а не дублируются здесь.
- name: Развернуть узел LPMC
  hosts: lpmc
  become: true
  roles:
    - lpmc_system
